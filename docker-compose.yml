services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: news_demo
    ports:
      - "5432:5432"
    volumes:
      - news_pgdata:/var/lib/postgresql/data

volumes:
  news_pgdata:
